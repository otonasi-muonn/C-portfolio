interface:
  display_name: "TDD Workflow (C-portfolio)"
  short_description: "C実装の変更を設計確認 + make all で検証"
  brand_color: "#22C55E"
  default_prompt: "docsを確認し、実装後にmake allで検証するTDDフローで進めて"
policy:
  allow_implicit_invocation: true

